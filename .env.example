PORT=8080
CONCURRENCY=4
MAX_RETRY=3
CHROME_POOL_SIZE=2
LOG_FORMAT=json

# MySQL (same DB as Python FastAPI)
DB_HOST=localhost
DB_PORT=3306
DB_USER=nightowl
DB_PASSWORD=nightowl
DB_NAME=nightowl

# Paths (auto-detected from ../night-owl/ if not set)
STORY_CONTENT_ROOT=../night-owl/story-content
SCRAPE_SOURCES_PATH=../night-owl/scrape_sources.json
